<%= form_for @landmark, html: { multipart: true } do |f| %>
  <% if @landmark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@landmark.errors.count, "error") %> prohibited this landmark from being saved:</h2>

      <ul>
      <% @landmark.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="form-group">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="form-group">
    <%= f.label :address %><br>
    <%= f.text_field :address %>
  </div>
  <div class="form-group">
    <%= f.label :criteria %><br>
    <%= f.text_field :criteria %>
  </div>
  <div class="form-group">
    <%= f.file_field :image %>

  </div>
  <div class="field">
    <input type="hidden" name="landmark[user_id]" value="<%= current_user.id%>" />
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
